{
  "name": "logger",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "library/go/logger",
  "tags": [],
  "targets": {
    "test": {
      "executor": "@nx-go/nx-go:test",
      "options": {
        "skipRace": true
      }
    },
    "lint": {
      "executor": "@nx-go/nx-go:lint"
    },
    "install": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "command": "go get -u {args.package}"
      }
    },
    "tidy": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "command": "go mod tidy"
      }
    },
    "download": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "command": "go mod download"
      }
    }
  }
}
